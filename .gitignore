# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# scratch dumps from DB inspection
steps.json

# Contains the Supabase service-role key and DB password. Never commit.
DB Extract/

# Superseded planning artifacts, copied back into the root when this session
# moved out of its scratch workspace. The live schema is supabase/migrations/
# (0001_core_tables.sql onward); these are the earlier drafts that produced it,
# plus the blueprint HTML. Kept on disk, out of the repo.
/0001_initial_schema.sql
/0002_seed_ganesha_from_extract.sql
/migrate-extract.mjs
/digital-purohit-blueprint.html
